name: Test Env

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print env variable
        run: echo "API_KEY is $API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}
